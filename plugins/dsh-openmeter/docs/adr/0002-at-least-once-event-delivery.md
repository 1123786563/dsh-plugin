# 用量事件至少一次投递（本地 WAL + 幂等重试）

插件在宿主侧维护磁盘持久队列（WAL），OpenMeter 不可达或重启期间用量事件不丢失，恢复后指数退避重试直至确认。安全性依据：OpenMeter 按（命名空间、事件来源、事件 ID）在 32 天窗口内去重，重复投递不会重复计费。理由：真实收款场景下丢事件 = 丢收入。

## Consequences

- 事件 ID 必须在生成时确定且重试时保持不变（派生自会话事件序号，而非投递时刻）。
- WAL 需要淘汰策略：已确认事件在超过 OpenMeter 去重窗口（32 天）后可清理。
